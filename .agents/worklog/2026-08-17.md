# 2026-08-17

## 01:30 CST — sohara-js 延后功能落地：完整宿主桥（record/sleep/notify/file/http/db/require + ctx + Record 方法）

- 变更（core/config/runtime）：`StepMeta{id,name,kind,step_type}` 与 `BuildContext{flow, step}` 落地；config/runtime/serve 构造点填充 flow，`build_partials` 逐 step 经 `step_context` 填充 `ctx.step`（去重：统一复用 `sohara-config::step_context`，graph.rs 删除本地副本）。
- 变更（sohara-js 重构成 6 模块）：`env.rs`（StepEnv：vars/flow/step/bus/permissions/db/state/emit/correlation_id + `require` 权限 + `resolve_module`；`BusHandle` 包装 `Arc<dyn EventBus>` 满足 quick-js 回调 `RefUnwindSafe` 约束）；`host.rs`（全部宿主回调，阻塞实现：log/env/var(带 fallback 原生 JSON)/now/uuid/sleep(clamp 0..60000)/notify(需 bus+权限)/file.read(恒允许)/file.write(需权限)/http.request(需权限，独立 OS 线程执行)/db.query(需权限+db 路径)/require/emit/state_sync/checkpoint(no-op)）；`callbacks.rs`（按组注册回调，JsValue 参数经 `Arguments` 变参转换）；`bridge.rs`（setup_context/run_script/`__call1`/`__call2` 入口调用/`invocation` 新鲜 correlation+emit/`merge_emitted` 语义：Pass→Expand(pass+emitted)、Filtered→Expand(emitted)、Expand 追加）；`step.rs`（ScriptStep/ScriptSource/ScriptSink 每次调用新建隔离 Context + 双参入口）；`assets/preamble.js`（`__RecordProto` 方法+id/timestamp/metadata getter、`__wrapRecord`、sohara 全桥、CommonJS require 缓存、`__makeCtx`、`__call1/__call2` 传真实 record 元信息、全局 `__ctx` 兼容）。
- 变更：`ScriptConfig` 新增 `allow`（权限列表）与 `db`（SQLite 路径）字段（deny_unknown_fields 严格校验）。
- 变更：测试——`tests/common/mod.rs`（共享 helper：registry/ctx/transform/inline/pass/expand/temp_dir/HTTP stub（独立线程最小 HTTP/1.1 服务器）/TestBus）；`tests/host_core.rs` 6 例（var/env fallback、ctx 身份、ctx.state 跨记录持久、Record 方法+元信息、emit 展开、require 模块）；`tests/host_io.rs` 10 例（file 读/写权限、http stub 往返+权限拒绝、db 建表插查+权限拒绝、notify 三态（无权限/无 bus/正常投递）、source ctx+emit）。
- 关键踩坑（实现约束）：(1) quick-js 回调参数须 `TryFrom<JsValue, Error=ValueError>`，JsValue 参数须走 `Arguments` 变参形式；(2) `Arc<dyn EventBus>` 不满足回调 `RefUnwindSafe`，需 `BusHandle` 包装；(3) `reqwest::blocking` 的 wait 逻辑会进入 debug shell runtime 探测，在 tokio 运行时上下文中直接调用即 panic（"Cannot drop a runtime…"），必须 `std::thread::scope` 挪到独立 OS 线程；(4) `sohara_core::Result` 别名与 `std::result::Result` 冲突需显式区分；(5) `str::as_str` 为 unstable（`str_as_str`），&str 直接 match。
- 验收：sohara-js 22 测试（6 script + 6 host_core + 10 host_io）全绿；workspace 47 套测试全绿；clippy -D warnings 0；fmt clean；文件/函数长度门禁全过（host 测试按 common/host_core/host_io 拆分、bridge 拆 callbacks.rs、parse_http_opts 拆 parse_http_headers、长测试按场景拆分）。
- 文档：`docs/design/quickjs-api.md` 状态列全面更新（§3 全表、§4 Record 方法、§5 ctx 表、§6 require、§7 同步基线已落地、§8 registerStep 仍延后、§10 权限细则）；延后项明确：`sohara.registerStep`、`ctx.state` checkpoint 持久化（本版进程内内存）。
- 待办：commit + push（git push --force 历史重写后的远端未变，本次普通 push）。

## 17:00 CST — 下一路线图规划：gap closure + 生态扩展

- 变更：新增 `docs/design/next-roadmap.md`（draft）—— 5 个 Phase（A–E）、14 个增量，覆盖 sohara-js 延后项修补（checkpoint/state 桥接、registerStep）、dylib 扩展接口（sohara-api crate + 7 个平台插件）、zero-config bootstrap（server 命令 + flow 管理 API + 持久化）、JS API DSL（FlowBuilder + 运行时解释）、Node.js/npm 工作流（@sohara/sdk + sohara-dev + scaffolding）。含 5 个开放决策（OD-1~OD-5）、6 条假设、明确排除项。
- 变更：`docs/design/extension-points.md` 更新—— §1（registerStep）和 §2（扩展接口）提升至 next-roadmap Phase A2/B；保留 AI agent 和其他候选锚点。
- 原因：用户要求分析 sohara-js 现状 + 4 个新方向（zero-config/dylib/npm/JS DSL），综合制定实现计划。
- 分析发现的关键断点：JS 桥的 `StepEnv.state` 与运行时 `Executor.states` + `StateStore` 完全断开；`BuildContext` 无 StateStore/checkpoint 回调；`host::checkpoint()` 是空函数。
- 验收：文档结构遵循 increment 技能（每增量含 prerequisites/actions/verification/complete-when）、restraint 技能（无多余实体、排除项明确）、agent-facing-doc 技能（可执行指导、自描述命名）。
- 参考：`docs/design/next-roadmap.md`（主文档）、`docs/design/extension-points.md`（已更新）。

## 17:30 CST — next-roadmap.md v2 修订（challenge 评审）

- 变更：`docs/design/next-roadmap.md` 升级为 v2，回应 9 条 challenge：
  1. **B1 FFI 设计重写**：放弃 `*mut dyn Extension` 跨 FFI（胖指针布局非 stable ABI），改为 feature-flag 静态链接为主路径；新增 A0b ABI spike 验证。
  2. **OD 表定稿**：OD-1 改为静态链接（与增量对齐）；OD-5 改为 full SDK（与 E1 对齐）；5 个 OD 全部标注定稿决策。
  3. **A1 状态单一权威**：`Executor.states` 为唯一权威副本；`state_sync` 写回内存，`checkpoint` 节流落盘（非逐条直写）。
  4. **A2 作用域模型定稿**：per-flow（非 per-context）；两遍构建解决声明顺序；需同步 `quickjs-api.md` §8。
  5. **A3 async spike 重写**：明确单线程死锁问题；机制描述改为非阻塞宿主调用 + 微任务泵；前置到 A1 之前（A0a）。
  6. **D2 线程模型**：独立 tokio runtime + OS 线程；serve 型流程返回 stop handle；删除 C2 依赖。
  7. **C 阶段**：C1 只做空启动；自动启动归 C3；`--admin-token` 强制；name 消毒；plane/agent 兼容说明。
  8. **E 阶段**：单实现（TypeScript 为权威，preamble.js 镜像）；二进制分发方案（PATH 查找 + 明确错误）；E2 依赖修正。
  9. **文档元数据**：修正 Supersedes 引用；新增 §7 Risks + §8 Overall Completion Criteria。
- 原因：challenge 发现 1 个 ABI 假设风险、2 处 OD 矛盾、多个语义未定义点。
- 参考：`docs/design/next-roadmap.md`（v2）。

## 18:00 CST — next-roadmap.md v2 challenge 自审 + vnv 全面校验

- 自审发现 7 个问题（3 高/中）：(1) OD-1 "extensions" 名不副实——静态链接要求重新编译，不是 drop-in 插件；(2) A0a 机制描述有结构性漏洞——JS 线程 condvar 等待期间无法泵微任务；(3) A1 StateAccessor 死锁风险未记录；(4) A2 两遍构建的注册通道未定义（StepFactory::build 签名无返回注册信息的途径）。
- vnv 发现关键新证据：quick-js crate 的 Promise 支持是**同步阻塞轮询**（`bindings.rs:980-1039`：注入 `.then()/.catch()` → 循环 `JS_ExecutePendingJob` → 检查全局 `__promiseResult` → 阻塞线程）。`JS_ExecutePendingJob` 不暴露给外部，无 Rust 侧创建/resolve Promise 的机制。A0a 描述的"非阻塞宿主调用 + condvar"方案在当前 crate 下不可行，需 fork crate 或改用 thenable 方案。
- vnv 结论：conditional pass——F1（A0a 不可行）为高，F2/F3/F4 为中，F5/F6 为低，F8/F9/F10 通过。最小修订集 5 条。
- 待修订后批准。

## 18:30 CST — next-roadmap.md v3 修订（challenge + vnv 5 条修订落地）

- 变更：`docs/design/next-roadmap.md` 升级为 v3，落地 5 条 vnv 修订：
  1. **F1 A0a 重写**：基于 quick-js crate `bindings.rs:980-1039` 的实际 Promise 机制（同步轮询，`JS_ExecutePendingJob` 不暴露），改为三条路径：fork crate / thenable workaround / 接受同步基线。A0a 不再 gate A1。
  2. **F2 OD-1 明确**：改为 "compile-time optional features"（非 runtime plugins），正视"需要重新编译二进制"的 trade-off。
  3. **F3 A1 死锁约束**：记录 `StateAccessor::set` 不能在 executor 持有 `states` 锁的路径上调用。
  4. **F4 A2 注册通道**：`BuildContext` 新增 `registered_steps: Arc<Mutex<Vec<RegisteredStep>>>` 字段；Pass 1 后 drain 注入 registry。
  5. **F5 E1 风险评级**：impact 从 Low 改为 High（sohara 不在 PATH 则 Phase E 完全失效）。
- 附带更新：A1 prerequisite 从"A0a"改为"none"；§6 假设对齐 OD-1；§8 完成标准更精确；执行顺序图更新；§9 变更记录新增。
- 参考：`docs/design/next-roadmap.md`（v3）。

## 22:59:20 CST — stdio 扩展主线前置并开始落地

- **变更**：`docs/design/next-roadmap.md` 升级为 draft v4，把 Phase B 从 compile-time/dylib 路线改为 **subprocess IPC / JSON-RPC over stdio**；`docs/design/extension-points.md` 同步标注 dylib deferred。代码侧新增 `sohara-runtime/src/stdio_extensions.rs`：支持扩展 manifest（yaml/json）加载、`initialize`/`transform` JSON-RPC 协议、external transform 注册；`sohara-cli/src/main.rs` 为 `run` / `serve` / `approve` 增加 `--extensions PATH`；`sohara-runtime/tests/stdio_extensions.rs` 增加外部 transform 集成测试。
- **原因**：用户明确选择优先推进 subprocess IPC 路线，以获得多语言扩展、ABI 隔离与更松耦合的交付方式。
- **验收**：`cargo test -p sohara-runtime --test stdio_extensions`、`cargo test -p sohara-cli`。
- **剩余**：当前仅支持 external transform；source/sink/trigger/state-store/event-bus 协议、timeout/restart policy、真实示例扩展目录仍待继续。

## 23:19:36 CST — stdio 扩展协议扩到 source / sink

- **变更**：扩展 `sohara-runtime/src/stdio_extensions.rs`，把 stdio host 从 transform-only 推进到 `source` / `transform` / `sink`：新增 `source.pull`、`sink.send`、`sink.flush` 协议；host 侧补齐 external source stream 与 external sink wrapper；`initialize.capabilities` 扩到 `source/transform/sink`。同步更新 `sohara-runtime/tests/stdio_extensions.rs`，增加 external source 与 external sink 集成场景；`docs/design/next-roadmap.md` 升级到 draft v5，`docs/design/extension-points.md` 同步当前 scope。
- **原因**：用户要求继续推进“扩展协议上的类型”，因此优先把最核心的数据步骤三类补齐，而不是继续停留在 transform-only。
- **验收**：`cargo test -p sohara-runtime --test stdio_extensions`、`cargo test -p sohara-cli`。
- **剩余**：trigger/state-store/event-bus 仍未进入协议；还缺 timeout/restart policy 与真实示例扩展目录。

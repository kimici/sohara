## 21:50 CST — vnv 修复 + 使用文档

- 变更：vnv 发现修复——(1) 设计文档 L122 `tags` 标注"延后，未实现"（此前与 D4 ✅ 行矛盾）；(2) 中继重放语义机制化：RelayBus 订阅者 id 从随机 uuid 改为稳定 id（serve 传 `--admin` 地址，缺省回退 uuid），plane 按订阅者保存游标下限（`relay_cursors`，内存）——实例进程重启不再重放已确认消息（新增测试 `relay_cursor_floor_prevents_replay_after_restart` 验证：同 id 重拉不重放、不同 id 独立游标）；§2.2/§9 明确定稿语义（plane 重启丢游标 → 重放邮箱尾部，需业务幂等键；持久化留 D5b）。
- 变更：新增 `docs/usage.md` 使用文档（构建/单机命令表/单机 Dashboard/持久化与幂等边界/分布式部署（plane+agent 配置、实例声明、Gateway 路由、跨机总线语义、Manager UI、三向 token 表）/常见问题排查 6 条）；README 增加 Usage 链接。
- 验收：122 测试全绿（+1 游标下限）、clippy 0、fmt、长度门禁全过。


## 22:10 CST — GitHub Actions：全量验证 CI + 四平台 tag 发布

- 变更：`ci.yml` 重写为纯验证管线（push 全分支+tag、PR 触发：fmt --check / clippy -D warnings / build --workspace --locked / test --locked / 两个长度门禁）；新增 `release.yml`（tag glob `v[0-9]+.[0-9]+.[0-9]+-*` 触发：四平台 matrix（win-x64/linux-x64/linux-arm64/apple-darwin-arm64，各自原生 runner）→ `cargo build --release --locked` 三个二进制 → zip/tar.gz 打包（Windows 用 bsdtar -a）→ upload-artifact → softprops/action-gh-release 发布 + generate_release_notes；release 前校验 tag 格式 `^v\d+\.\d+\.\d+-[A-Za-z0-9]+$`）；`docs/usage.md` 增「CI 与发布」节（产物命名表 + 打 tag 示例）、README Development 段更新。
- 验证：两份 YAML 解析通过；release 构建与打包在本地按 workflow 命令复现（三二进制 release 构建 + tar.gz 打包内容正确）；tag 格式正则对正反例（v0.1.0-alpha ✓ / v1.2.3 ✗）行为符合预期。GitHub 上未实际运行过（无远端执行证据），首次推送后需观察。

## 22:25 CST — 修正：vXX.YY.ZZ（无后缀）tag 也触发发布

- 变更：release.yml 触发 glob 改为 `v[0-9]+.[0-9]+.[0-9]+*`，校验正则改为 `^v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9]+)?$`（后缀可选）；校验步骤前置到 release-build job 首步（不合规 tag 直接失败、不再浪费四平台构建）；usage.md/README 同步说明。
- 验证：YAML 解析通过；正反例：v0.1.0-alpha ✓ / v1.2.3 ✓ / v2025.08.16-hotfix2 ✓；release-1.0 ✗ / v1.2.3-alpha.1 ✗ / v1.2 ✗。
